# Per PRD §13.5 - set real values per environment in the host dashboard, never commit them.
DATABASE_URL=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
SESSION_SECRET=
APP_BASE_URL=

# First-admin bootstrap: read on startup only when no active admin exists.
# Once an admin exists these are ignored (and can be removed from the host).
ADMIN_NAME=
ADMIN_EMAIL=
ADMIN_PASSWORD=
